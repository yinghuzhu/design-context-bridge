# New page

## User prompt

在目标仓库新增 `/campaign`，design-platform URL 为用户给出的节点；无旧业务页，保留现有导航和鉴权约定。

## Accepted mapping

模式为 `new`，目标是 `/campaign`，项目现有 layout 是可复用直接依赖。

## Allowed files

目标 route、页面、直接组件、样式、测试与明确引用的 layout。

## Forbidden scope expansion

不遍历其他页面或组件库，不自行选择参考设计。

## State changes

实现后记录 target；视觉和业务均通过后才写 validated。

## Evidence

多模态 Agent 对比原稿截图与实际截图，并保存构建和路由检查结果。

## Expected final report

报告实现文件、真实 URL、截图证据、业务检查和剩余低优先级差异。
